name: Fullstack CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # ---------------------------
      # Backend CI (server folder)
      # ---------------------------
      - name: Install Backend Dependencies
        working-directory: server  # ✅ Remove ./
        run: npm install

      - name: Lint Backend
        working-directory: server  # ✅ Remove ./
        run: npx prettier --check .

      - name: Run Backend Tests
        working-directory: server  # ✅ Remove ./
        run: npm test || echo "No backend tests yet"

      - name: Build Backend (if needed)
        working-directory: server  # ✅ Remove ./
        run: npm run build || echo "No backend build"

      # ---------------------------
      # Frontend CI (client folder)
      # ---------------------------
      - name: Install Frontend Dependencies
        working-directory: client  # ✅ Remove ./
        run: npm install

      - name: Lint Frontend
        working-directory: client  # ✅ Remove ./
        run: npx prettier --check .

      - name: Run Frontend Tests
        working-directory: client  # ✅ Remove ./
        run: npm test || echo "No frontend tests yet"

      - name: Build Frontend
        working-directory: client  # ✅ Remove ./
        run: npm run build || echo "No frontend build"
